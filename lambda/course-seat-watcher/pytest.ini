[pytest]
# Lets tests do `from config import ...` / `import handler` etc. without a
# package structure -- matches how the Dockerfile flattens these modules
# into ${LAMBDA_TASK_ROOT} (no package __init__.py at runtime either).
pythonpath = .
