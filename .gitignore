# Local tooling caches — not project content
.atl/
.codegraph/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# checkov --download-external-modules cache
.external_modules/

# Node / Maizzle (email/) -- build output is baked into the Lambda image at
# Docker build time, not committed.
node_modules/
email/build_local/
email/build_production/
